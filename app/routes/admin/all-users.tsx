import { Header } from "@/components";

type Props = {};

const AllUsers = (props: Props) => {
  return (
    <main className="dashboard wrapper">
      <Header title="Trips Page" description="Manage Users" />
      Content
    </main>
  );
};

export default AllUsers;
